include: [component: $CI_SERVER_FQDN/lc-components/id_tokens/id_tokens-component@main]

variables:
  LLNL_SLURM_SCHEDULER_PARAMETERS: '--nodes=1 --reservation=ci --userns --exclusive'
  VERSION: 'latest'
  GIT_SUBMODULE_STRATEGY: recursive

build:
  stage: build
  tags:
    - dane
    - batch
  script:
    - /usr/bin/enable-podman
    - echo "${CI_REGISTRY_PASSWORD}" | podman login -u="$CI_REGISTRY_USER" --password-stdin $CI_REGISTRY
    - mkdir -p data
    # adding DATA_PATH into current podman build context
    - cp -r $DATA_PATH/* data
    # Version management (use tag if available, or commit SHA otherwise)
    - SERV_REV="${CI_COMMIT_TAG:-$CI_COMMIT_SHA}"
    - echo "Server version $SERV_REV"
    - podman build --rm --pull-always --force-rm --cgroup-manager=cgroupfs --build-arg DATA_PATH=data --build-arg SERVER_VERSION=$SERV_REV -t "$CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG" .
    # Push the branch-specific tag
    - podman push "$CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG"
    # Explicitly create the latest tag locally
    - podman tag "$CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG" "$CI_REGISTRY_IMAGE:latest"
    # Push the latest tag
    - podman push "$CI_REGISTRY_IMAGE:latest"
    # Push tag into SD Nexus Registry
    - echo "Pushing image into SD Nexus Container Registry"
    - echo $NEXUS_PASS | podman login -u "$NEXUS_USER" --password-stdin $NEXUS_REGISTRY
    - podman tag "$CI_REGISTRY_IMAGE:latest" "${NEXUS_REGISTRY_IMAGE}:${VERSION}"
    - podman push "${NEXUS_REGISTRY_IMAGE}:${VERSION}"
    # Explicitly create the demo tag
    - podman tag "$CI_REGISTRY_IMAGE:latest" ${NEXUS_REGISTRY_IMAGE}:demo
    - podman push ${NEXUS_REGISTRY_IMAGE}:demo
    - podman logout $NEXUS_REGISTRY
    - podman logout $CI_REGISTRY

deploy-dev:
  stage: deploy
  tags:
    - shell
    - oslic
  script:
    - echo "Deploying FLASK-Copilot Development App in K8s server"
    - /usr/global/openshift/bin/oc login --server=$K8S_SERVER_API --token=$FLASK_DEPLOYER_TOKEN
    # Restart Dev Deployment
    - /usr/global/openshift/bin/oc rollout restart deployment $DEPLOYMENT_NAME
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop"'
      when: on_success

deploy:
  stage: deploy
  tags:
    - shell
    - oslic
  script:
    - echo "Deploying FLASK-Copilot App in K8s server"
    - /usr/global/openshift/bin/oc login --server=$K8S_SERVER_API --token=$FLASK_DEPLOYER_TOKEN
    # Restart Prod Deployment
    - /usr/global/openshift/bin/oc rollout restart deployment $DEPLOYMENT_NAME_PROD
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: on_success
    - if: $CI_COMMIT_TAG # Runs only when a Git tag is created
